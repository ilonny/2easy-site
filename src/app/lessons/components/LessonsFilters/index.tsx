import { Tabs } from "@nextui-org/react";
import { FC, useMemo } from "react";

type TProps = {
  tabIndex: "userLessons" | "savedLessons";
};

export const LessonsFilters: FC<TProps> = ({ tabIndex }) => {
  return <></>;
};
