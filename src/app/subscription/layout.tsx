import React from "react";

export const metadata = {
  title: "Подписка",
  description:
    "3 дня бесплатного доступа к функционалу платформы и всем материалам",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
