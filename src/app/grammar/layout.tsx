import React from "react";

export const metadata = {
  title: "Grammar",
  description: "Разговорные упражнения и игры для отработки грамматических тем",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
