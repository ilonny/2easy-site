import React from "react";

export const metadata = {
  title: "Speaking games",
  description: "Игры для любой части занятия с juicy лексикой",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
